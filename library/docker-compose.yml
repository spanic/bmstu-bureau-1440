services:
  postgres:
    image: postgres:18-alpine
    container_name: library-postgres
    environment:
      POSTGRES_DB: library
      POSTGRES_USER: library
      POSTGRES_PASSWORD: library
    ports:
      - "5432:5432"
    volumes:
      - library-data:/var/lib/postgresql

volumes:
  library-data:
