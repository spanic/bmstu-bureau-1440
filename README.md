# BMSTU x Бюро 1440 — Индустриальная разработка ПО (2025)

## Задания

| № | Тема                     | Запрос на слияние                                           | Элемент курса                                               | Оценка | Отчет ✨                                                  | Lessons learned                                                                                                                                                                                           |
|:-:|:-------------------------|:------------------------------------------------------------|-------------------------------------------------------------|--------|----------------------------------------------------------|-----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1 | Приложение банка         | [PR №1](https://github.com/spanic/bmstu-bureau-1440/pull/1) | [Ссылка](https://lms.bmstu.ru/mod/assign/view.php?id=39617) |        |                                                          |                                                                                                                                                                                                           |
| 2 | Приложение для зоопарка  | [PR №2](https://github.com/spanic/bmstu-bureau-1440/pull/2) | [Ссылка](https://lms.bmstu.ru/mod/assign/view.php?id=39609) | 5.3/10 | [Смотреть](#зоопарк)                                     | ☑️ нужен более подробный отчет (с примерами кода);<br/>☑️ необходимо **строгое** соответствие критериям оценки в задании;<br/>☑️ оценка всегда субъективная, подробной **обратной связи нет и не будет**. |
| 3 | БД – домашнее задание №1 | [PR №3](https://github.com/spanic/bmstu-bureau-1440/pull/3) | [Ссылка](https://lms.bmstu.ru/mod/assign/view.php?id=40117) |        | [Смотреть](#современные-базы-данных--домашнее-задание-1) |                                                                                                                                                                                                           |                                                                                                                                                                                                           |

---

### Зоопарк

🎥 Скринкаст: [ссылка](https://disk.yandex.kz/i/x96DSBSVanI3Ag)

🪧 Доска с описанием архитектуры: [ссылка](https://unidraw.io/app/board/6b180f2544ddd1fd7237?allow_guest=true)

Программа моделирует работу зоопарка: составление отчётов, учет персонала, уборку, кормление и лечение животных. Работа
с программой
осуществляется через консольное меню, реализованы автодополнение при выборе и обработка некорректных действий
пользователя.

**Принципы SOLID, использованные при разработке архитектуры:**

- **Single Responsibility Principle** – при рассмотрении классов `Animal` и `ReportGenerator` видно, что у каждого из
  них есть своя специализация: `Animal` хранит информацию о состоянии и поведении животного, а `ReportGenerator` создает
  отчёты, и их зоны ответственности не пересекаются

- **Open/Closed Principle** – имеется возможность легко добавлять новых животных, создавая новых наследников класса
  `Animal`, не
  меняя, например, логику их кормления или лечения, а также логику составления отчетов

- **Liskov Substitution Principle** – базовый класс `Animal` легко заменяется любым его наследником: несмотря на то,
  что,
  например,
  метод уборки ожидает именно `Animal`, он корректно работает и с классом `Tiger`, и с классом `Parrot`

- **Interface Segregation Principle** – классы сотрудников реализуют только те интерфейсы, которые им необходимы,
  отсутствуют
  методы без реализации

- **Dependency Inversion Principle** – модуль отчетности ожидает объект класса, реализующего интерфейс `ProvidesInfo`,
  что
  дает
  возможность легко добавлять новые виды отчетов и менять их во время выполнения программы

> ⚠️ При дальнейшем расширении описания предметной области, скорее всего, возникнут некоторые трудности при выстраивании
> полноценной классификации животных и делении их на хищников, травоядных, млекопитающих и другие их виды, т. к. это
> затрагивает, к тому же, логику кормления животных. Кроме того, на данный момент сделано допущение, что в каждом
> вольере
> находится только одно животное, что влияет на логику уборки – при дальнейшем проектировании системы это тоже
> необходимо будет учесть.

Тем не менее использование принципов SOLID в архитектуре программы уже сейчас позволяет легко её расширять, создавая
новые классы сотрудников, животных, и добавляя новые виды отчетов.

### Современные базы данных – домашнее задание №1

#### Общая схема БД (кратко)

В базе данных используются следующие основные сущности:

- `clients` — клиенты банка
- `accounts` — банковские счета клиентов
- `cards` — карты, привязанные к счетам
- `transactions` — операции по счетам
- `loans` — кредиты клиентов
- `loan_payments` — платежи по кредитам

Связи:

`clients → accounts` — один клиент может иметь несколько счетов

`accounts → cards` — у одного счёта может быть несколько карт

`accounts → transactions `— у счёта может быть много операций

`clients → loans `— у клиента может быть несколько кредитов

`loans → loan_payments` — у кредита может быть несколько платежей

#### Запросы

1. **Активные счета в EUR, открытые после 2024-01-01**

    ```postgres-psql
    SELECT *
    FROM accounts
    WHERE
        opened_at > '2024-01-01'
        AND currency = 'EUR'
        AND status = 'active'
    ORDER BY opened_at;
    ```

   **Что делает запрос:**

   Запрос выбирает активные счета в евро, открытые после 1 января 2024 года, и сортирует их по дате открытия.

   **Источник данных**

    - Таблица: `accounts`

   **Разбор условий WHERE**

    - `opened_at > '2024-01-01'` — фильтрация по дате открытия;
    - `currency = 'EUR'` — отбор счетов в евро;
    - `status = 'active'` — учитываются только активные счета.


2. **ФИО клиента, тип счета, валюта и статус**

    ```postgres-sql
    SELECT clients.full_name, accounts.account_type, accounts.currency, accounts.status
    FROM clients
        JOIN accounts ON clients.client_id = accounts.client_id;
    ```

   **Что делает запрос**

   Выводит информацию о клиентах только тех, у кого есть хотя бы один счёт.

   **Используемые таблицы**

    - `clients`

    - `accounts`

   **Тип JOIN**

    - `JOIN (INNER JOIN)`

   **Почему INNER JOIN:**

   Нужно показать только клиентов, у которых существуют счета. Клиенты без счетов автоматически исключаются.

   **Связь таблиц**

   `clients.client_id = accounts.client_id`


3. **Все клиенты и количество их счетов (включая 0)**

    ```postgres-sql
    SELECT clients.*, COUNT(accounts.account_id) AS account_count
    FROM clients
        LEFT JOIN accounts ON clients.client_id = accounts.client_id
    GROUP BY
        clients.client_id;
    ```

   **Что делает запрос**

   Показывает всех клиентов, даже если у них нет счетов, и считает количество счетов для каждого.

   **Тип JOIN**

    - `LEFT JOIN`

   **Почему LEFT JOIN:**

   Нужно сохранить всех клиентов. Если счетов нет — **COUNT** вернёт `0`.

   **GROUP BY**

   Группировка по `clients.client_id`, чтобы считать счета отдельно для каждого клиента.


4. **Клиенты с более чем 2 активными счетами**

    ```postgres-sql
    SELECT clients.*, COUNT(accounts.account_id) AS account_count
    FROM clients
        JOIN accounts ON clients.client_id = accounts.client_id
    WHERE
        accounts.status = 'active'
    GROUP BY
        clients.client_id
    HAVING
        COUNT(accounts.account_id) > 2
    ORDER BY account_count DESC;
    ```

   **Логика запроса**

    1. Берём только активные счета;
    2. Считаем их количество для каждого клиента;
    3. Оставляем клиентов, у которых активных счетов больше 2.

   **Отличие `WHERE` и `HAVING`**

    - `WHERE` — фильтрует строки до агрегации;
    - `HAVING` — фильтрует после `GROUP BY`.


5. **Счета с входящими операциями выше среднего по банку**

    ```postgres-sql
    SELECT accounts.*, sum(transactions.amount)
    FROM accounts
        JOIN transactions ON accounts.account_id = transactions.account_id
    WHERE
        transactions.txn_type IN ('deposit', 'transfer_in')
    GROUP BY
        accounts.account_id
    HAVING
        sum(transactions.amount) > (
            SELECT avg(amount)
            FROM transactions
            WHERE
                txn_type IN ('deposit', 'transfer_in')
        );
    ```

   **Что делает запрос**

   Hаходит счета, у которых сумма входящих операций выше среднего значения по всем счетам банка.

   **Подзапрос**
    ```postgres-sql
    SELECT avg(amount)
    FROM transactions
    WHERE
        txn_type IN ('deposit', 'transfer_in')
    ```

   Считает среднюю сумму входящих операций по всей таблице `transactions`.

   **Почему `HAVING`**

    - Сравнение происходит с агрегатной функцией `SUM`, поэтому используется `HAVING`.


6. **Топ-5 клиентов по обороту за 2025 год**

    ```postgres-sql
    SELECT clients.*, sum(transactions.amount) AS transactions_sum
    FROM
        clients
        JOIN accounts ON clients.client_id = accounts.client_id
        JOIN transactions ON accounts.account_id = transactions.account_id
    WHERE
        transactions.txn_date >= '2025-01-01'
        AND transactions.txn_date < '2026-01-01'
    GROUP BY
        clients.client_id
    ORDER BY transactions_sum DESC
    LIMIT 5;
    ```

   **Что такое оборот**

   Оборот — это сумма всех операций, независимо от типа.

   **Логика**

    1. Фильтрация операций за 2025 год;
    2. Группировка по клиенту;
    3. Сортировка по сумме операций;
    4. Ограничение до 5 записей.


7. **Определение активности клиента за 90 дней**

   **Цель запроса**

   Для каждого клиента определить уровень активности на основе количества операций за последние 90 дней:

    - `0` операций → `inactive`;
    - `1–5` → `low`;
    - `6–20` → `medium`;
    - `> 20` → `high`.

    ```postgres-sql
    WITH
        transactions_per_user AS (
            SELECT clients.*, count(transactions.transaction_id) transactions_count
            FROM
                clients
                LEFT JOIN accounts ON clients.client_id = accounts.client_id
                LEFT JOIN transactions ON transactions.account_id = accounts.account_id
                AND transactions.txn_date > now() - interval '90 days'
            GROUP BY
                clients.client_id
        )
    SELECT
        *,
        CASE
            WHEN transactions_count = 0 THEN 'inactive'
            WHEN transactions_count BETWEEN 1 AND 5  THEN 'low'
            WHEN transactions_count BETWEEN 6 AND 20  THEN 'medium'
            ELSE 'high'
        END activity
    FROM transactions_per_user
    ORDER BY transactions_count DESC;
    ```

   **Что происходит по шагам**

    1. **CTE `transactions_per_user`**

       CTE (Common Table Expression) создаёт временную результирующую таблицу, где для каждого клиента считается
       количество
       операций.

       **Таблицы:**

        - `clients` — базовая таблица;
        - `accounts` — счета клиента;
        - `transactions` — операции по счетам.

       **JOIN’ы:**

        - `clients LEFT JOIN accounts` → чтобы сохранить клиентов без счетов;
        - `accounts LEFT JOIN transactions` → чтобы сохранить клиентов без операций;

       Важный момент ❗

       ```postgres-sql
       AND transactions.txn_date > now() - interval '90 days'
       ```

       Это условие находится в `ON`, а не в `WHERE`.

       Почему это важно:

        - Если бы условие было в `WHERE`, клиенты без операций исчезли бы;
        - В `ON` — клиенты остаются, а `transactions` просто будут `NULL`.

    2. **`COUNT(transactions.transaction_id)`**

        - `COUNT` считает только не-`NULL` значения;
        - Если операций нет → `COUNT = 0`.

    3. **`CASE`**

       Преобразует число операций в бизнес-категорию активности.

   **Зачем CTE**

    - Повышает читаемость;
    - Легко расширять (например, добавить сумму операций);
    - Удобно отлаживать.


8. **Кредиты, по которым выплачено менее 50% от суммы кредита**

    ```postgres-sql
    SELECT loans.*, COALESCE(sum(loan_payments.amount), 0) total_payments
    FROM loans
        LEFT JOIN loan_payments ON loans.loan_id = loan_payments.loan_id
        AND loan_payments.status = 'success'
    GROUP BY
        loans.loan_id
    HAVING
        COALESCE(sum(loan_payments.amount), 0) < loans.principal * 0.5;
    ```

   **Цель запроса**

   Найти кредиты, где сумма успешных платежей меньше 50% от тела кредита (principal).

   **Подробный разбор**

    1. **`LEFT JOIN` с `loan_payments`**

       Используется `LEFT JOIN`, потому что:

        - кредит может существовать;
        - но платежей по нему может не быть вообще.

    2. **Фильтрация по статусу платежа**
        ```postgres-sql
        AND loan_payments.status = 'success'
        ```

       Важно: фильтр находится в `ON`, а не в `WHERE`.

       👉 Это позволяет:

        - учитывать кредиты без платежей;
        - учитывать только успешные платежи.

    3. **`COALESCE`**
       ```postgres-sql
       COALESCE(sum(loan_payments.amount), 0)
       ```

       Почему это нужно:

        - `SUM(NULL)` → `NULL`;
        - сравнивать `NULL < число` нельзя;
        - `COALESCE` заменяет `NULL` на `0`.

    4. **`HAVING`**

       Используется для фильтрации по агрегатному значению (`SUM`).

    5. **Бизнес-смысл**

       Запрос выявляет проблемные кредиты, по которым клиент выплатил менее половины суммы.


9. **Активные карты и их владельцы**

    ```postgres-sql
    SELECT clients.full_name, accounts.account_id, cards.issued_at, cards.expires_at
    FROM cards
        JOIN accounts ON cards.account_id = accounts.account_id
        JOIN clients ON accounts.client_id = clients.client_id
    WHERE
        cards.status = 'active';
    ```

   **Цель запроса**

   Показать все активные карты и:

    - кому они принадлежат;
    - к какому счёту относятся;
    - срок действия карты.

   **Логика JOIN’ов**

    1. `cards → accounts`

       Карта всегда привязана к счёту

    2. `accounts → clients`

       Счёт всегда принадлежит клиенту

   **Используется `INNER JOIN`, потому что:**

    - активная карта не может существовать без счёта
    - счёт не может существовать без клиента

   **Типичный вопрос от преподавателя**

   > Почему не LEFT JOIN?

   Ответ: потому что нас интересуют только реальные владельцы активных карт, а не абстрактные записи без связей. Да и
   вообще, нас интересует не реальное, рабочее решение, а чтобы было оформлено красиво, эталон для нас – нейрослоп с
   красивыми бесполезными циферками. Обратной связи не будет. Возражения не принимаются.


10. **Количество операций и сумма списаний по каждому счёту**

    ```postgres-sql
    SELECT
        accounts.*,
        count(transactions.transaction_id) transactions_amount,
        sum(
            CASE
                WHEN transactions.txn_type IN (
                    'withdrawal',
                    'transfer_out',
                    'fee'
                ) THEN transactions.amount
                ELSE 0
            END
        ) withdrawals_sum
    FROM accounts
        LEFT JOIN transactions ON accounts.account_id = transactions.account_id
    GROUP BY
        accounts.account_id;
    ```

    **Цель запроса**

    Для каждого счёта получить:

    - общее количество операций;
    - сумму списаний (не всех операций).

    **Почему `LEFT JOIN`**

    Чтобы показать счета без операций – у таких счетов `COUNT = 0`, `SUM = 0`

    **`COUNT(transactions.transaction_id)`**

    - считает все операции по счёту;
    - `NULL` не учитываются.

    **`CASE` внутри `SUM` — ключевой момент**

    ```postgres-sql
    SUM(
        CASE
            WHEN txn_type IN (...) THEN amount
            ELSE 0
        END
    )
    ```

    Что это даёт:
    - строки не отбрасываются;
    - суммируются только нужные типы операций;
    - остальные операции учитываются как 0.

    👉 Это лучше, чем фильтровать через `WHERE`, потому что:

    - `WHERE` убрал бы строки;
    - `COUNT` стал бы некорректным.

    **Бизнес-логика**

    Запрос позволяет:
    - оценить активность счёта;
    - понять объём списаний;
    - использовать данные для анализа риска и доходности.